# Lista de sementes — um perfil por linha, `#` comenta.
#
# Aceita HANDLE (recomendado) ou DID:
#   fulano.bsky.social
#   @fulano.bsky.social      o @ é opcional
#   fulano                   sem domínio vira fulano.bsky.social
#   jornal.com.br            domínio próprio funciona
#   did:plc:abc123           se você já tem o DID
#
# `nabote seeds --file <arquivo>` resolve os handles e registra tudo.
# Handle que não existe é reportado e não derruba o resto da lista.
#
# Copie este arquivo, troque pelas suas escolhas curadas (frente 02 do plano)
# e mantenha versionado: a lista de sementes é o ativo de curadoria do projeto.

# --- exemplo (contas oficiais do próprio Bluesky, para testar o fluxo) ---
bsky.app
